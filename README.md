# untitled-role-finder-db

This is a service that retrieves job roles from an API and stores them in a database.
- Start up the database
`cd Docker`
`docker compose up`

- To retrieve roles from the Jobs API, we need to setup the api token.
`setx API_TOKEN "yourapitoken"`